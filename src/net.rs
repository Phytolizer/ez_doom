impl crate::state::State {
    pub fn dedicated_server(&mut self) -> ! {
        // TODO
        std::process::exit(0);
    }

    pub fn master_query(&mut self) -> ! {
        // TODO
        std::process::exit(0);
    }

    pub fn query_address(&mut self, _address: &str) -> ! {
        // TODO
        std::process::exit(0);
    }

    pub fn lan_query(&mut self) -> ! {
        // TODO
        std::process::exit(0);
    }
}
